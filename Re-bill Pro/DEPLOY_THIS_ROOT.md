# DEPLOY THIS FOLDER AT THE REPOSITORY ROOT

This ZIP is intentionally flat. `server.js`, `admin.html`, `package.json`, and `railway.json` are at the ZIP root.
Replace the files at your actual GitHub/Railway service root with these files. Do not upload this as a nested `Re-bill Pro/backend` folder.

The Admin Sign out build is `20260828-admin-signout-runtime-enforced-4`.
