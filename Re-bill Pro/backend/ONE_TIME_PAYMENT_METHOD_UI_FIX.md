# One-time payment method UI fix

For customers with zero subscriptions, the customer drawer now hides the `Selected by` row.
The saved payment method section only shows the card, funding, country, and expiry.
Subscription customers keep the existing `Selected by` source label.
